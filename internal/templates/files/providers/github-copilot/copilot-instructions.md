# GitHub Copilot Instructions

Follow `AGENTS.md` if it exists in the repository. Prefer small safe changes, existing project patterns, targeted verification, and clear closeout with summary, files changed, verification, and risks.

If RTK was initialized by Leaserage, its official hook rewrites supported shell commands automatically where the host supports it. If only prompt-level guidance is available and `rtk` is installed, prefer `rtk <command>` for noisy checks.
