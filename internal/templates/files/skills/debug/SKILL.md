---
name: debug
description: Use when something is broken, failing, or behaving unexpectedly.
---

# Debug

Reproduce the issue first, isolate the smallest failing path, inspect evidence before changing code, then make the smallest fix and verify the original failure is gone.
